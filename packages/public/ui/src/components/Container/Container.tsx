import Container, { ContainerProps } from '@material-ui/core/Container';
import React from 'react';

const Footer = (props: ContainerProps): JSX.Element => <Container maxWidth="xl" {...props} />;

export default Footer;
